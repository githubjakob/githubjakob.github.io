title: Jakob Zanker
subtitle: Software Developer
author: Jakob Zanker
email: hello@jakobzanker.de
url: https://jakobzanker.de
baseurl: "" # subpath of the site, e.g. "/blog" (leave it blank "" if you're site shouldn't use a subpath)
description: > # description of the site (multiple lines allowed)
  Software Developer, based in Munich

permalink: /:slug.html

favicon: "icon.png" # name+extension of favicon (which must be put on the root folder)

theme_config:
  appearance: "auto" # can be "light", "dark" or "auto"
  back_home_text: ".. back" # customize text for homepage link in post layout
  date_format: "%Y-%m-%d" # customize how date is formatted
  show_description: false # show blog description in home page

sass:
  style: :compressed

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-redirect-from